@page "{id:Guid}"
@model EntryModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <div>
        This entry expires in @Model.ExpiresIn
    </div>
    <div>
        @Model.TextContent
    </div>
</div>
