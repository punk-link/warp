{{- if .Values.victoriaMetrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.victoriaMetrics.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "victoriametrics.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - name: http
    port: {{ .Values.victoriaMetrics.ports.http }}
    targetPort: http
    protocol: TCP
  selector:
    {{- include "victoriametrics.selectorLabels" . | nindent 4 }}
{{- end }}
