/** Enumeration of possible expiration periods for entries. */
export enum ExpirationPeriod {
	FiveMinutes = 'FiveMinutes',
	ThirtyMinutes = 'ThirtyMinutes',
	OneHour = 'OneHour',
	EightHours = 'EightHours',
	OneDay = 'OneDay'
}
