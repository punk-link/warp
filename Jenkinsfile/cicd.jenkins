node {
    def app

    stage('Clone repository') {
        /* Let's make sure we have the repository cloned to our workspace */

        checkout scm
    }

stage('Build image') {
                echo 'Starting to build docker image'

                script {
                    def customImage = docker.build("registry.punk.link/warp-webapp:${env.GIT_COMMIT}-DEV")
                    /* customImage.push() */
                }
            }
}

