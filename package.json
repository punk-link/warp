{
  "scripts": {
    "lint:client": "cd Warp.ClientApp && yarn install && yarn lint",
    "format:client": "cd Warp.ClientApp && yarn install && yarn format",
    "typecheck:client": "cd Warp.ClientApp && yarn install && yarn typecheck",
    "e2e:client": "cd Warp.ClientApp && yarn install && yarn e2e",
    "compose:e2e:up": "docker compose -f docker-compose.yml -f docker-compose.e2e.yml up --build",
    "compose:e2e:down": "docker compose -f docker-compose.yml -f docker-compose.e2e.yml down --volumes"
  },
  "dependencies": {}
}
